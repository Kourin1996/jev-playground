# Copy to .dev.vars for local development. Never commit real credentials.
# The model is pinned for reproducible evaluation; do not use a moving `latest` alias.
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-1.13.0
